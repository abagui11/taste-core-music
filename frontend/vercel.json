{
  "version": 2,
  "builds": [{
    "src": "package.json",
    "use": "@vercel/node-builder",
    "config": {
      "installCommand": "npm install",
      "buildCommand": "npm run build"
    }
  }],
  "outputDirectory": "build"
}